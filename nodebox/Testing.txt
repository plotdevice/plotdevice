Inferface testing

Open an animation script.
Export without running first to an image.
Export without running first to a movie.

Open a static script.
Export without running first to an image.
Export without running first to a movie.

Open an animation script.
Zoom in.
Export to an image.
Export to a movie.

Make a NodeBox animation without a draw() command.
Export one image frame. (should give an error)
Make the draw command
Export one image frame. (Should export the one frame)

Stop while rendering an animation. The animation should have all frames up till that point.